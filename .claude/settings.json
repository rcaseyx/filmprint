{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "branch=$(git branch --show-current); if [ \"$branch\" = \"main\" ]; then echo \"ERROR: You are on main. Create a branch first (git checkout -b <branch-name>)\" >&2; exit 1; fi"
          }
        ]
      }
    ]
  }
}
