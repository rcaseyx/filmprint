[build]
watchPaths = ["Dockerfile", "requirements.txt", "pyproject.toml", "filmprint/**", "api/**", "scripts/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
